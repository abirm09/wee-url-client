import DemoUserCredentialsCopy from "@/features/auth/login/DemoUserCredentialsCopy";

const CardInfoCopy = ({ value }: { value: string }) => {
  return <DemoUserCredentialsCopy value={value} />;
};

export default CardInfoCopy;
