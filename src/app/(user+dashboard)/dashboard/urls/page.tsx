import { Box } from "@/components";
import { AllURL } from "@/features/dashboard";

const page = () => {
  return (
    <div>
      <Box>
        <AllURL />
      </Box>
    </div>
  );
};

export default page;
